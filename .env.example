# Optional: Ollama base URL (only needed if using local mode)
# Default: http://localhost:11434
NEXT_PUBLIC_OLLAMA_BASE_URL=http://localhost:11434

# Note: OpenAI API keys should NOT be stored in environment variables
# Users enter their API key directly in the UI
# The key is stored in localStorage and sent only to our API route via custom header
